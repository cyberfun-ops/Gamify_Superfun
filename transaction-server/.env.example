DB_HOST=localhost
DB_PORT=5432
DB_NAME=gamify_auth
DB_USER=postgres
DB_PASSWORD=your_db_password

JWT_SECRET=your_jwt_secret

# Secret header value expected from UroPay / SMS webhook calls
# Set this in your UroPay webhook config as the X-Webhook-Secret header value
WEBHOOK_SECRET=change_this_to_a_random_secret

PORT=5001
NODE_ENV=development
